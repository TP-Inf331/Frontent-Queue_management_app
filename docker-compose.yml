services:
  app:
    build: .
    image: queue-management-system:latest
    container_name: queue-management-app
    ports:
      - '4000:80'
    restart: always
    environment:
      - VITE_API_URL=http://localhost:3000

networks:
  default:
    driver: bridge
